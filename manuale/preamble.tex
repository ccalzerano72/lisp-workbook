% =============================================================
%  PREAMBLE — Common Lisp: Guida Pratica
%  Compilare con: pdflatex main  (x2)
% =============================================================

% --- Encoding e lingua ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[italian]{babel}

% --- Font ---
\usepackage{lmodern}
\usepackage{microtype}
\usepackage{graphicx}   % per \scalebox nelle stelle
\usepackage[outline]{contour}  % per bordino bianco sulle stelle
\contourlength{0.6pt}

% --- Layout pagina ---
\usepackage[
  a4paper,
  top=2.5cm, bottom=2.8cm,
  left=2.5cm, right=2.5cm
]{geometry}

% --- Colori ---
\usepackage[dvipsnames,svgnames,table]{xcolor}

\definecolor{clBg}{RGB}{248,248,248}       % sfondo listing
\definecolor{clKeyword}{RGB}{0,0,180}      % keyword Lisp
\definecolor{clComment}{RGB}{100,100,100}  % commenti
\definecolor{clString}{RGB}{0,120,0}       % stringhe

% Colori difficoltà
\definecolor{colOne}{RGB}{130,130,130}     % ★      grigio
\definecolor{colTwo}{RGB}{34,110,34}       % ★★     verde
\definecolor{colThree}{RGB}{30,80,160}     % ★★★    blu
\definecolor{colFour}{RGB}{200,100,0}      % ★★★★   arancio
\definecolor{colFive}{RGB}{180,30,30}      % ★★★★★  rosso

% Colori box esercizi (3 livelli)
\definecolor{boxMinFrame}{RGB}{30,80,160}      % Minimo: blu
\definecolor{boxMinBack}{RGB}{235,241,252}
\definecolor{boxStdFrame}{RGB}{34,110,34}      % Standard: verde
\definecolor{boxStdBack}{RGB}{236,248,236}
\definecolor{boxColFrame}{RGB}{110,110,110}    % Collaterale: grigio
\definecolor{boxColBack}{RGB}{245,245,245}

% Colori box ausiliari
\definecolor{boxHintFrame}{RGB}{180,130,0}
\definecolor{boxHintBack}{RGB}{255,252,220}
\definecolor{boxSolFrame}{RGB}{34,110,34}
\definecolor{boxSolBack}{RGB}{236,248,236}
\definecolor{boxRagFrame}{RGB}{140,50,140}
\definecolor{boxRagBack}{RGB}{248,236,248}

% Alias legacy per compatibilita'
\definecolor{boxEssFrame}{RGB}{30,80,160}
\definecolor{boxEssBack}{RGB}{235,241,252}

% Colori semantici (cheat sheet e quick reference)
\definecolor{csBlue}{RGB}{30,80,160}      % Fondamenti, stile
\definecolor{csGreen}{RGB}{34,110,34}     % Liste, sequenze
\definecolor{csOrange}{RGB}{200,100,0}    % Controllo flusso
\definecolor{csPurple}{RGB}{130,50,150}   % Astrazione, OOP
\definecolor{csTeal}{RGB}{0,110,110}      % Strutture dati, I/O

% --- Matematica ---
\usepackage{amsmath}
\usepackage{amssymb}

% --- Listings (codice Lisp) ---
\usepackage{listings}

\lstdefinelanguage{CommonLisp}{%
  sensitive=false,
  morecomment=[l]{;},
  morestring=[b]",
  morekeywords=[1]{%
    defun, defmacro, defparameter, defvar, defconstant,
    defstruct, defclass, defmethod, defgeneric,
    let, let*, flet, labels, setf, setq,
    if, cond, when, unless, case, ecase,
    and, or, not,
    progn, prog1, prog2,
    loop, dotimes, dolist, do, do*,
    lambda, function,
    funcall, apply,
    mapcar, maplist, mapcan, mapc,
    remove-if, remove-if-not, find-if, find-if-not,
    every, some, notany, notevery,
    reduce, count-if, count,
    sort, stable-sort,
    return, return-from,
    values, multiple-value-bind, multiple-value-list,
    destructuring-bind,
    handler-case, handler-bind, error, warn, signal,
    with-open-file, open, close,
    format, print, princ, prin1, terpri, fresh-line,
    read, read-line, read-char,
    car, cdr, cons, list, append, reverse, length,
    first, second, third, fourth, fifth,
    rest, last, nth, nthcdr, butlast,
    null, atom, listp, consp, numberp, stringp,
    symbolp, functionp, integerp, floatp, characterp,
    eq, eql, equal, equalp,
    quote, backquote,
    make-hash-table, gethash, remhash, maphash, hash-table-count,
    make-array, aref, array-dimensions,
    make-instance, slot-value, initialize-instance,
    in-package, defpackage, use-package, export, import,
    compile, load, require, provide,
    t, nil%
  },
  morekeywords=[2]{%
    +, -, *, /, mod, rem, expt, sqrt, abs,
    floor, ceiling, truncate, round,
    min, max, 1+, 1-,
    =, /=, <, >, <=, >=,
    char, char-code, code-char, char-upcase, char-downcase,
    string, string-upcase, string-downcase, string-length,
    coerce, type-of, typep, check-type%
  }
}

\lstdefinestyle{lispstyle}{%
  language=CommonLisp,
  basicstyle=\ttfamily\small,
  keywordstyle=[1]{\color{clKeyword}\bfseries},
  keywordstyle=[2]{\color{clKeyword}},
  commentstyle=\itshape\color{clComment},
  stringstyle=\color{clString},
  backgroundcolor=\color{clBg},
  frame=single,
  framesep=4pt,
  framerule=0.4pt,
  rulecolor=\color{gray!50},
  xleftmargin=8pt,
  xrightmargin=4pt,
  breaklines=true,
  breakatwhitespace=true,
  showstringspaces=false,
  tabsize=2,
  numbers=none,
  upquote=true,
  extendedchars=true,
  literate=
    {→}{{$\rightarrow$}}1
    {★}{{\textcolor{colOne}{$\bigstar$}}}1
    {à}{{\`a}}1  {è}{{\`e}}1  {é}{{\'{e}}}1  {ì}{{\`{\i}}}1
    {ò}{{\`o}}1  {ù}{{\`u}}1
    {À}{{\`A}}1  {È}{{\`E}}1  {É}{{\'{E}}}1
    {Ì}{{\`I}}1  {Ò}{{\`O}}1  {Ù}{{\`U}}1
}

\lstset{style=lispstyle}

% Shorthand per listing inline
\newcommand{\cl}[1]{\lstinline[style=lispstyle]{#1}}

% --- tcolorbox ---
\usepackage[most]{tcolorbox}
\tcbuselibrary{listings, breakable, skins}

% Stelle di difficoltà — con bordino bianco tramite pacchetto contour
% \contour{white}{...} traccia il simbolo in bianco in 8 direzioni
% poi sovrappone il simbolo colorato: risultato = alone bianco netto
\newcommand{\stellaC}[1]{%
  \contour{white}{\textcolor{#1}{$\bigstar$}}%
}
\newcommand{\unaStella}{\stellaC{colOne}}
\newcommand{\dueStelle}{\stellaC{colTwo}\stellaC{colTwo}}
\newcommand{\treStelle}{\stellaC{colThree}\stellaC{colThree}\stellaC{colThree}}
\newcommand{\quattroStelle}{\stellaC{colFour}\stellaC{colFour}\stellaC{colFour}\stellaC{colFour}}
\newcommand{\cinqueStelle}{\stellaC{colFive}\stellaC{colFive}\stellaC{colFive}\stellaC{colFive}\stellaC{colFive}}

% Mappa numero di stelle → colore frame
\newcommand{\starcolor}[1]{%
  \ifcase#1
    \color{colOne}%   0 (non usato)
  \or\color{colOne}%  1
  \or\color{colTwo}%  2
  \or\color{colThree}% 3
  \or\color{colFour}% 4
  \else\color{colFive}% 5
  \fi
}

% ---- Comandi semantici per il testo degli esercizi ----
% Testo discorsivo: grigio e piccolo
\newcommand{\extext}[1]{{\small\color{black!70}#1}}
% Etichette sezione interna: bold colorato
\newcommand{\exlabel}[1]{\textbf{\textcolor{black!75}{#1}}}
% Vincolo: evidenziato con colore
\newcommand{\vincolo}[1]{\smallskip\noindent{\small\textcolor{boxMinFrame!80!black}{\textbf{Vincolo:}} \color{black!70}#1}}
% Nota: come vincolo ma più neutro
\newcommand{\exnota}[1]{\smallskip\noindent{\small\textcolor{black!60}{\textit{#1}}}}

% ---- Comandi semantici per suggerimenti ----
% Titolo dell'esercizio nell'hint
\newcommand{\hinttitle}[1]{\textbf{\textcolor{boxHintFrame!80!black}{#1}}}
% Passo / step numerato
\newcommand{\hintstep}[1]{\smallskip\noindent{\small\textcolor{boxHintFrame!70!black}{\textbf{#1}}}\enspace}
% Schema/attenzione
\newcommand{\hintschema}[1]{\smallskip\noindent{\small\textbf{\textcolor{black!65}{#1}}}\enspace}

% ---- Comandi semantici per soluzioni ----
% "Soluzione di riferimento"
\newcommand{\solriferimento}{\noindent{\small\textit{\textcolor{black!55}{Soluzione di riferimento --- non unica.}}}\smallskip}
% Sezione interna ragionamento (Rappresentazione, Complessità ecc.)
\newcommand{\raglabel}[1]{\smallskip\noindent{\small\textbf{\textcolor{boxRagFrame!80!black}{#1}}}\enspace}

% ---- Box ESERCIZIO MINIMO ◆ (blu) ----
% Uso: \begin{minbox}{n}{titolo}{stelle} ... \end{minbox}
\newenvironment{minbox}[3]{%
  \medskip
  \begin{tcolorbox}[
    enhanced,
    colframe=boxMinFrame,
    colback=boxMinBack,
    fonttitle=\bfseries\small,
    title={{\small\textcolor{boxMinFrame}{$\blacklozenge$}}\enspace Esercizio~#1\enspace---\enspace#2\enspace
      \hfill\ifcase#3\or\unaStella\or\dueStelle\or\treStelle\or\quattroStelle\or\cinqueStelle\fi},
    arc=3pt,
    boxrule=1.2pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=12pt plus 3pt,
    after skip=12pt plus 3pt,
    parskip=5pt
  ]
  \small\color{black!75}%
}{%
  \end{tcolorbox}
  \medskip
}

% ---- Box ESERCIZIO STANDARD ■ (verde) ----
% Uso: \begin{stdbox}{n}{titolo}{stelle} ... \end{stdbox}
\newenvironment{stdbox}[3]{%
  \medskip
  \begin{tcolorbox}[
    enhanced,
    colframe=boxStdFrame,
    colback=boxStdBack,
    fonttitle=\bfseries\small,
    title={{\small\textcolor{boxStdFrame}{$\blacksquare$}}\enspace Esercizio~#1\enspace---\enspace#2\enspace
      \hfill\ifcase#3\or\unaStella\or\dueStelle\or\treStelle\or\quattroStelle\or\cinqueStelle\fi},
    arc=3pt,
    boxrule=1pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=12pt plus 3pt,
    after skip=12pt plus 3pt,
    parskip=5pt
  ]
  \small\color{black!75}%
}{%
  \end{tcolorbox}
  \medskip
}

% ---- Box ESERCIZIO COLLATERALE ○ (grigio) ----
% Uso: \begin{colbox}{n}{titolo}{stelle} ... \end{colbox}
\newenvironment{colbox}[3]{%
  \medskip
  \begin{tcolorbox}[
    enhanced,
    colframe=boxColFrame,
    colback=boxColBack,
    fonttitle=\bfseries\small,
    title={{\small\textcolor{boxColFrame}{$\circ$}}\enspace Esercizio~#1\enspace---\enspace#2\enspace
      \hfill\ifcase#3\or\unaStella\or\dueStelle\or\treStelle\or\quattroStelle\or\cinqueStelle\fi},
    arc=3pt,
    boxrule=0.8pt,
    borderline={0.8pt}{0pt}{boxColFrame, dashed},
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=12pt plus 3pt,
    after skip=12pt plus 3pt,
    parskip=5pt
  ]
  \small\color{black!75}%
}{%
  \end{tcolorbox}
  \medskip
}

% Alias legacy: essbox -> minbox
\newenvironment{essbox}[3]{\begin{minbox}{#1}{#2}{#3}}{\end{minbox}}

% ---- Box SUGGERIMENTO ----
\newenvironment{hintbox}[1]{%
  \medskip
  \begin{tcolorbox}[
    enhanced,
    breakable,
    colframe=boxHintFrame,
    colback=boxHintBack,
    fonttitle=\bfseries\small,
    title={Suggerimento --- Esercizio~#1},
    arc=3pt,
    boxrule=0.8pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=12pt plus 3pt,
    after skip=12pt plus 3pt,
    parskip=5pt,
    lines before break=3
  ]
  \small\color{black!75}%
}{%
  \end{tcolorbox}
  \medskip
}

% ---- Box SOLUZIONE ----
\newenvironment{solbox}[1]{%
  \medskip
  \begin{tcolorbox}[
    enhanced,
    breakable,
    colframe=boxSolFrame,
    colback=boxSolBack,
    fonttitle=\bfseries\small,
    title={Soluzione --- Esercizio~#1},
    arc=3pt,
    boxrule=0.8pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=12pt plus 3pt,
    after skip=12pt plus 3pt,
    parskip=5pt,
    break at=15cm,
    lines before break=4
  ]
  \small\color{black!75}%
}{%
  \end{tcolorbox}
  \medskip
}

% ---- Box RAGIONAMENTO (per ★★★★/★★★★★) ----
\newenvironment{ragionamentoBox}[1]{%
  \smallskip
  \begin{tcolorbox}[
    enhanced,
    breakable,
    colframe=boxRagFrame,
    colback=boxRagBack,
    fonttitle=\bfseries\small,
    title={Ragionamento --- Esercizio~#1},
    arc=3pt,
    boxrule=0.8pt,
    left=10pt, right=10pt, top=8pt, bottom=8pt,
    toptitle=3pt, bottomtitle=3pt,
    before skip=8pt,
    after skip=8pt,
    parskip=5pt,
    lines before break=3
  ]
  \small\color{black!70}%
}{%
  \end{tcolorbox}
  \smallskip
}

% Alias brevi per compatibilita' (comandi -> ambienti)
% Non usare questi per listing, usare direttamente gli ambienti
\newcommand{\essenzialeBox}[4]{\begin{essbox}{#1}{#2}{#3}#4\end{essbox}}
\newcommand{\collateraleBox}[4]{\begin{colbox}{#1}{#2}{#3}#4\end{colbox}}
\newcommand{\hintBox}[2]{\begin{hintbox}{#1}#2\end{hintbox}}
\newcommand{\soluzioneBox}[2]{\begin{solbox}{#1}#2\end{solbox}}

% --- Intestazioni capitolo ---
\usepackage{titlesec}
\usepackage{titletoc}   % per personalizzare il formato del TOC

% --- Formato TOC ---
% \part: prominente, colore primario, riga di separazione
\titlecontents{part}[0em]
  {\addvspace{1.5em}\bfseries\Large\color{boxEssFrame}}
  {\partname~\thecontentslabel\enspace---\enspace}
  {}
  {\hfill\contentspage}
  [\addvspace{0.2em}\hrule\addvspace{0.5em}]

% \chapter numerato: rientro 2em, grassetto normale (piu' piccolo della parte)
\titlecontents{chapter}[2em]
  {\addvspace{0.3em}\bfseries\normalsize}
  {\contentslabel{2em}}
  {\hspace*{-2em}\bfseries}  % non numerati (Suggerimenti, Soluzioni): nessun offset
  {\hfill\contentspage}

% \section: rientro 4.5em, font piccolo, puntini
\titlecontents{section}[4.5em]
  {\small}
  {\contentslabel{2em}}
  {\hspace*{-2em}}
  {\titlerule*[0.4pc]{.}\contentspage}
\titleformat{\chapter}[display]
  {\normalfont\Large\bfseries\color{boxEssFrame}}
  {\chaptertitlename\ \thechapter}{10pt}{\Huge}
\titlespacing*{\chapter}{0pt}{20pt}{30pt}

% --- Header/Footer ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\nouppercase{\leftmark}}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\fancypagestyle{plain}{%
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
}

% --- Hyperref (deve essere quasi l'ultimo) ---
\usepackage[
  colorlinks=true,
  linkcolor=boxEssFrame,
  urlcolor=colTwo,
  citecolor=colThree,
  bookmarksnumbered=true,
  bookmarksopen=true,
  pdftitle={Common Lisp --- Guida Pratica},
  pdfauthor={},
  pdfsubject={Common Lisp},
  pdfkeywords={Common Lisp, programmazione funzionale, esercizi}
]{hyperref}

% --- Multicol per cheat sheet ---
\usepackage{multicol}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{dashrule}   % per \hdashrule nel cheat sheet

% Impaginazione: evita vedove e orfane
\widowpenalty=10000
\clubpenalty=10000

% Etichette esercizi, hint, soluzioni
% Uso: \label{ex:42}  \label{hint:42}  \label{sol:42}

% Comandi utili
\newcommand{\kw}[1]{\texttt{\bfseries #1}}  % keyword inline
\newcommand{\fn}[1]{\texttt{#1}}            % funzione inline
\newcommand{\result}{{\color{clComment}; $\rightarrow$\ }}

% Nomi simboli per legenda
\newcommand{\minimo}{{\color{boxMinFrame}$\blacklozenge$}}
\newcommand{\standard}{{\color{boxStdFrame}$\blacksquare$}}
\newcommand{\collateral}{{\color{boxColFrame}$\circ$}}

% Alias legacy
\newcommand{\essential}{\minimo}
\newcommand{\optional}{\collateral}

% =============================================================
%  Sezioni separate per Suggerimenti e Soluzioni
%  Usano contatori propri per evitare conflitti con Esercizi
% =============================================================
\newcounter{hintsec}
\newcounter{solsec}

% Sezione numerata per Suggerimenti (con voce in indice)
% Uso: \hintsection{Titolo}
\newcommand{\hintsection}[1]{%
  \stepcounter{hintsec}%
  \vspace{1.5em}%
  \noindent{\large\bfseries\thehintsec\quad #1}%
  \addcontentsline{toc}{section}{\protect\numberline{\thehintsec}#1}%
  \par\vspace{0.8em}%
}

% Sezione numerata per Soluzioni (con voce in indice)
% Uso: \solsection{Titolo}
\newcommand{\solsection}[1]{%
  \stepcounter{solsec}%
  \vspace{1.5em}%
  \noindent{\large\bfseries\thesolsec\quad #1}%
  \addcontentsline{toc}{section}{\protect\numberline{\thesolsec}#1}%
  \par\vspace{0.8em}%
}
