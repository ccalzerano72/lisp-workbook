% =============================================================
%  00_cheatsheet.tex — Common Lisp Cheat Sheet
%  Design con box colorati e layout arioso
% =============================================================
\chapter*{Cheat Sheet}
\addcontentsline{toc}{chapter}{Cheat Sheet}
\markboth{Cheat Sheet}{Cheat Sheet}

% Colori semantici cs* definiti in preamble.tex

% Box sezione colorato
\newtcolorbox{cheatbox}[2][]{%
  enhanced,
  colback=#2!8,
  colframe=#2!80!black,
  fonttitle=\bfseries\sffamily,
  coltitle=white,
  title={#1},
  boxrule=0.8pt,
  arc=4pt,
  left=5pt, right=5pt, top=3pt, bottom=3pt,
  toptitle=2pt, bottomtitle=2pt,
  before skip=6pt,
  after skip=6pt
}

% Descrizione in grigio
\newcommand{\desc}[1]{{\color{black!55}\small #1}}

% Riga di tabella più compatta
\renewcommand{\arraystretch}{1.15}

% ============================================================
%  PAGINA 1
% ============================================================

\begin{multicols}{2}
\small

% ==================== COLONNA 1 ====================

% ---- Definizioni (BLU) --------------------------------
\begin{cheatbox}[Definizioni e binding]{csBlue}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(defun f (x) corpo)}      & \desc{funzione} \\
\fn{(defparameter *v* e)}     & \desc{globale} \\
\fn{(let ((x e)...) corpo)}   & \desc{binding locale} \\
\fn{(let* ((x e)...) corpo)}  & \desc{binding seq.} \\
\fn{(setf place val)}         & \desc{assegnamento} \\
\end{tabular}
\end{cheatbox}

% ---- Booleani (BLU) -----------------------------------
\begin{cheatbox}[Booleani e logica]{csBlue}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{T} / \fn{NIL}             & \desc{vero / falso} \\
\fn{(and a b ...)}            & \desc{e logico} \\
\fn{(or a b ...)}             & \desc{o logico} \\
\fn{(not x)}                  & \desc{negazione} \\
\end{tabular}
\end{cheatbox}

% ---- Condizioni (BLU) ---------------------------------
\begin{cheatbox}[Condizioni]{csBlue}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(if test then else)}      & \desc{ramo singolo} \\
\fn{(cond (t1 e1)...)}        & \desc{rami multipli} \\
\fn{(when test corpo...)}     & \desc{solo se vero} \\
\fn{(unless test corpo...)}   & \desc{solo se falso} \\
\fn{(case v (k1 e1)...)}      & \desc{switch} \\
\end{tabular}
\end{cheatbox}

% ---- Liste (VERDE) ------------------------------------
\begin{cheatbox}[Liste]{csGreen}
\textit{\footnotesize Costruzione:}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{'(1 2 3)}                 & \desc{quoted} \\
\fn{(list a b ...)}           & \desc{crea lista} \\
\fn{(cons x lst)}             & \desc{prependi} \\
\fn{(append l1 l2)}           & \desc{concatena} \\
\fn{(reverse lst)}            & \desc{inverti} \\
\end{tabular}

\smallskip
\textit{\footnotesize Accesso:}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(car lst)} / \fn{(first lst)} & \desc{primo} \\
\fn{(cdr lst)} / \fn{(rest lst)}  & \desc{resto} \\
\fn{(nth n lst)}              & \desc{n-esimo (0-ix)} \\
\fn{(last lst)}               & \desc{ultima cella} \\
\fn{(length lst)}             & \desc{lunghezza} \\
\fn{(member x lst)}           & \desc{appartiene?} \\
\end{tabular}
\end{cheatbox}

\columnbreak

% ==================== COLONNA 2 ====================

% ---- Aritmetica (ARANCIONE) ---------------------------
\begin{cheatbox}[Aritmetica]{csOrange}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{+ - * /}                  & \desc{base} \\
\fn{(mod a b)} / \fn{(rem a b)} & \desc{resto} \\
\fn{(1+ x)} / \fn{(1- x)}     & \desc{inc / dec} \\
\fn{(expt b e)} / \fn{(sqrt x)} & \desc{potenza / radice} \\
\fn{(abs x)}                  & \desc{valore assoluto} \\
\fn{(max ...)} / \fn{(min ...)} & \desc{max / min} \\
\fn{(floor x)} / \fn{(ceiling x)} & \desc{troncamenti} \\
\fn{(round x)}                & \desc{arrotonda} \\
\end{tabular}

\smallskip
\hdashrule{\linewidth}{0.3pt}{2pt}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{= /= < > <= >=}           & \desc{confronti numerici} \\
\end{tabular}
\end{cheatbox}

% ---- Predicati (ARANCIONE) ----------------------------
\begin{cheatbox}[Predicati]{csOrange}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(null x)}                 & \desc{NIL?} \\
\fn{(atom x)} / \fn{(consp x)} & \desc{atomo? / cons?} \\
\fn{(listp x)}                & \desc{lista?} \\
\fn{(numberp x)} / \fn{(symbolp x)} & \desc{tipo?} \\
\fn{(stringp x)}              & \desc{stringa?} \\
\end{tabular}

\smallskip
\hdashrule{\linewidth}{0.3pt}{2pt}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(eq a b)}                 & \desc{identità} \\
\fn{(eql a b)}                & \desc{+ num/char} \\
\fn{(equal a b)}              & \desc{strutturale} \\
\fn{(equalp a b)}             & \desc{+ case-insens.} \\
\end{tabular}
\end{cheatbox}

% ---- Stringhe (VERDE) ---------------------------------
\begin{cheatbox}[Stringhe]{csGreen}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(length s)}               & \desc{lunghezza} \\
\fn{(char s i)}               & \desc{i-esimo char} \\
\fn{(subseq s start end)}     & \desc{sottostringa} \\
\fn{(concatenate 'string ...)} & \desc{concatena} \\
\fn{(string-upcase s)}        & \desc{MAIUSCOLO} \\
\fn{(string-downcase s)}      & \desc{minuscolo} \\
\fn{(parse-integer s)}        & \desc{str→int} \\
\fn{(string= s1 s2)}          & \desc{uguaglianza} \\
\end{tabular}
\end{cheatbox}

\end{multicols}

\clearpage

% ============================================================
%  PAGINA 2
% ============================================================

\begin{multicols}{2}
\small

% ==================== COLONNA 1 ====================

% ---- Iterazione (VERDE) -------------------------------
\begin{cheatbox}[Iterazione]{csGreen}
\textit{\footnotesize Base:}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(dotimes (i n) ...)}      & \desc{i = 0..n-1} \\
\fn{(dolist (x lst) ...)}     & \desc{su lista} \\
\end{tabular}

\smallskip
\textit{\footnotesize Loop:}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{loop for x in lst do}     & \desc{su lista} \\
\fn{loop for i from a to b}   & \desc{numerico} \\
\fn{loop ... collect e}       & \desc{raccogli} \\
\fn{loop ... sum e}           & \desc{somma} \\
\fn{loop ... when test}       & \desc{filtro} \\
\end{tabular}
\end{cheatbox}

% ---- Higher-order (VIOLA) -----------------------------
\begin{cheatbox}[Higher-order]{csPurple}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{\#'nome}                  & \desc{rif.\ a funzione} \\
\fn{(lambda (x) corpo)}       & \desc{anonima} \\
\fn{(funcall f a b)}          & \desc{chiama f} \\
\fn{(apply f lista)}          & \desc{f su lista args} \\
\end{tabular}

\smallskip
\hdashrule{\linewidth}{0.3pt}{2pt}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(mapcar \#'f lst)}        & \desc{trasforma} \\
\fn{(remove-if \#'p lst)}     & \desc{rimuovi se p} \\
\fn{(remove-if-not \#'p lst)} & \desc{tieni se p} \\
\fn{(find-if \#'p lst)}       & \desc{primo match} \\
\fn{(count-if \#'p lst)}      & \desc{conta} \\
\fn{(every \#'p lst)}         & \desc{tutti?} \\
\fn{(some \#'p lst)}          & \desc{almeno uno?} \\
\fn{(reduce \#'f lst)}        & \desc{fold} \\
\fn{(sort lst \#'<)}          & \desc{ordina (distr.!)} \\
\end{tabular}
\end{cheatbox}

% ---- Valori multipli (VIOLA) --------------------------
\begin{cheatbox}[Valori multipli]{csPurple}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(values v1 v2 ...)}       & \desc{ritorna più val} \\
\fn{(multiple-value-bind}     & \\
\fn{\ \ (a b) expr body)}     & \desc{cattura valori} \\
\end{tabular}
\end{cheatbox}

% ---- Macro (VIOLA) ------------------------------------
\begin{cheatbox}[Macro]{csPurple}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(defmacro m (p) corpo)}   & \desc{definisci} \\
\fn{`(... ,e ,@lst)}          & \desc{backquote} \\
\fn{(gensym)}                 & \desc{simbolo unico} \\
\fn{(macroexpand-1 '(m ...))} & \desc{espandi} \\
\end{tabular}
\end{cheatbox}

\columnbreak

% ==================== COLONNA 2 ====================

% ---- Array (TEAL) -------------------------------------
\begin{cheatbox}[Array e vettori]{csTeal}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{\#(1 2 3)}                & \desc{vettore letterale} \\
\fn{(make-array n :initial-element v)} & \desc{vettore} \\
\fn{(make-array '(r c) :initial-element v)} & \desc{matrice} \\
\fn{(aref a i)}               & \desc{accesso} \\
\fn{(setf (aref a i) v)}      & \desc{assegna} \\
\end{tabular}
\end{cheatbox}

% ---- Strutture (TEAL) ---------------------------------
\begin{cheatbox}[Strutture]{csTeal}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(defstruct nome s1 s2)}   & \desc{definisci} \\
\fn{(make-nome :s1 v1)}       & \desc{crea} \\
\fn{(nome-s1 istanza)}        & \desc{leggi slot} \\
\fn{(setf (nome-s1 ist) v)}   & \desc{modifica} \\
\fn{(nome-p x)}               & \desc{predicato tipo} \\
\end{tabular}
\end{cheatbox}

% ---- Alist (TEAL) -------------------------------------
\begin{cheatbox}[Association list]{csTeal}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{'((k1 . v1) (k2 . v2))}   & \desc{struttura} \\
\fn{(assoc key alist)}        & \desc{cerca} \\
\fn{(cdr (assoc k al))}       & \desc{ottieni valore} \\
\end{tabular}
\end{cheatbox}

% ---- Hash table (TEAL) --------------------------------
\begin{cheatbox}[Hash table]{csTeal}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(make-hash-table)}        & \desc{crea} \\
\fn{(gethash k ht)}           & \desc{leggi} \\
\fn{(setf (gethash k ht) v)}  & \desc{scrivi} \\
\fn{(remhash k ht)}           & \desc{rimuovi} \\
\fn{(maphash \#'f ht)}        & \desc{itera (f k v)} \\
\end{tabular}
\end{cheatbox}

% ---- I/O (TEAL) ---------------------------------------
\begin{cheatbox}[Input/Output]{csTeal}
\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{(read)}                   & \desc{leggi S-expr} \\
\fn{(read-line)}              & \desc{leggi riga} \\
\fn{(print x)}                & \desc{stampa + newline} \\
\fn{(princ x)}                & \desc{stampa leggibile} \\
\fn{(format t "..." ...)}     & \desc{formattato} \\
\end{tabular}

\smallskip
\textit{\footnotesize Direttive format:}
\smallskip

\begin{tabular}{@{}l@{\hspace{8pt}}l@{}}
\fn{\textasciitilde A}        & \desc{qualsiasi valore} \\
\fn{\textasciitilde D} / \fn{\textasciitilde F} & \desc{intero / float} \\
\fn{\textasciitilde\%}        & \desc{newline} \\
\end{tabular}
\end{cheatbox}

\end{multicols}
